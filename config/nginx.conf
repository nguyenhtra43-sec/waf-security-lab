server {
    listen 80 default_server;
    listen [::]:80 default_server;

    # Bật WAF cho virtual host này
    modsecurity on;

    location / {
        default_type text/html;
        return 200 "OK - WAF Security Lab Active";
    }
}