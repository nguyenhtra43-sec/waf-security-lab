server {
    listen 80 default_server;
    listen [::]:80 default_server;

    # Kích hoạt ModSecurity (Image sẽ tự động include toàn bộ rules và unicode.mapping)
    modsecurity on;

    location / {
        default_type text/html;
        return 200 "OK - WAF Security Lab Active";
    }
}