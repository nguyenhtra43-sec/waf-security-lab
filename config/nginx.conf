server {
    listen 80 default_server;
    listen [::]:80 default_server;

    # Bật ModSecurity Engine
    modsecurity on;

    # Chỉ ghi đè duy nhất directive ép ModSecurity chặn request (403)
    modsecurity_rules '
        SecRuleEngine On
    ';

    location / {
        default_type text/html;
        return 200 "OK - WAF Security Lab Active";
    }
}