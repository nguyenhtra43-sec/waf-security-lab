server {
    listen 80 default_server;
    listen [::]:80 default_server;

    # Bật ModSecurity engine (các quy tắc CRS đã được nạp tự động)
    modsecurity on;

    location / {
        default_type text/html;
        return 200 "OK - WAF Security Lab Active";
    }
}