server {
    listen 80 default_server;
    listen [::]:80 default_server;

    # Gọi trực tiếp file config được entrypoint sinh ra
    include /etc/nginx/conf.d/modsecurity.conf;

    location / {
        default_type text/html;
        return 200 "OK - WAF Security Lab Active";
    }
}